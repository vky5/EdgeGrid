# NATS Connection URL
NATS_URL=nats://localhost:4222

# Comma-separated list of supported embedding models
SUPPORTED_MODELS=all-minilm

# Optional: Unique identifier for the worker (auto-generated if left empty)
WORKER_ID=
